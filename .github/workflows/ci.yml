name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.12.0'
          cache: npm

      - name: Apply consolidated UI system once
        if: github.event_name == 'pull_request'
        run: |
          node scripts/apply-ui-system-integration.mjs
          git add index.html scripts/public-assets.mjs sw.js netlify.toml app-version.js package.json package-lock.json tests/scan-integration.test.mjs ux-client.js
          if git diff --cached --quiet; then
            echo "UI system wiring already applied."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git commit -m "style: wire consolidated UI system"
            git push origin HEAD:${{ github.head_ref }}
          fi

      - name: Install dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund

      - name: Static validation
        run: npm run lint

      - name: Tests
        run: npm test

      - name: Production build
        run: npm run build

      - name: Verify deploy output
        run: |
          test -f dist/index.html
          test -f dist/ui-system.css
          test -f dist/app-version.js
          test -f dist/user-isolation.js
          test -f dist/intake-core.js
          test -f dist/intake-client.js
          test -f dist/scan-core.js
          test -f dist/scan-client.js
          test -f dist/printer-core.js
          test -f dist/printer-dashboard.js
          test -f dist/app.js
          test -f dist/audit-core.js
          test -f dist/audit-client.js
          test -f dist/personal-core.js
          test -f dist/personal-dashboard.js
          test -f dist/sw.js
          test ! -e dist/package.json
          test ! -e dist/netlify.toml
          test ! -e dist/netlify

      - name: Upload deploy artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: filamentinventory-dist-${{ github.run_id }}
          path: dist
          if-no-files-found: error
          retention-days: 7
