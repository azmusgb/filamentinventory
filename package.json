{
  "name": "filamentinventory",
  "private": true,
  "version": "10.1.0",
  "description": "Local-first private filament inventory PWA with streamlined mobile UX, smart intake, QR workflows, contextual and bulk spool operations, Printer/AMS command center, secure sync, labels, and per-user customization.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "lint": "node scripts/validate.mjs",
    "test": "node --experimental-strip-types --test",
    "build": "node scripts/build.mjs",
    "ci": "npm run lint && npm test && npm run build"
  },
  "dependencies": {
    "@netlify/blobs": "11.0.1",
    "qrcode": "1.5.4"
  },
  "devDependencies": {
    "@netlify/functions": "6.0.0",
    "@types/qrcode": "1.5.5"
  }
}
