{
  "name": "filamentinventory",
  "private": true,
  "version": "9.6.0",
  "description": "Local-first per-user filament inventory PWA with isolated Bill/Aimee data, smart mobile intake, fast QR spool lookup, private Printer/AMS command center, secure sync, QR labels, and per-user UX customization.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "lint": "node scripts/validate.mjs",
    "test": "node --experimental-strip-types --test",
    "build": "node scripts/build.mjs",
    "ci": "npm run lint && npm test && npm run build"
  },
  "dependencies": {
    "@netlify/blobs": "11.0.1",
    "qrcode": "1.5.4"
  },
  "devDependencies": {
    "@netlify/functions": "6.0.0",
    "@types/qrcode": "1.5.5"
  }
}
