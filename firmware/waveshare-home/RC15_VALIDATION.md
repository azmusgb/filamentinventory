# rc15 validation

This marker triggers a push-based firmware validation build after the rc15 Bambu LAN discovery engine landed on main, allowing the normal workflow-run release publisher to publish the validated rc15 application image.
